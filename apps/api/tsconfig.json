{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // O empacotamento é feito pelo esbuild; o tsc aqui só valida tipos.
    "noEmit": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "vitest.config.ts"]
}
